# Project-specific ProGuard rules for release builds.
#
# The restored app state references this file from the Android release
# configuration. Keeping it present avoids release packaging failures even
# when no custom rules are currently required.
